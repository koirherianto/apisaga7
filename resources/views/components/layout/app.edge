<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title>{{ title || "Your default title" }}</title>
  <meta name="description" content="{{ title || 'Apisaga' }}">

  @if ($slots.meta)
    {{{ await $slots.meta() }}}
  @endif

  @vite(['resources/css/app.css', 'resources/js/app.js'])
</head>
<body>
  {{{ await $slots.main() }}}
</body>
</html>
